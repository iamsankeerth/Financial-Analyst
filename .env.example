# Copy to `.env` and fill in your own key (do not commit `.env`).
GOOGLE_API_KEY=YOUR_GOOGLE_API_KEY_HERE

# Optional model overrides
GEMINI_MODEL=gemini-3.0-flash
GEMINI_MEMORY_MODEL=gemini-3.0-flash-001
